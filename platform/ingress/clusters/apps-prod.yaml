# Single-node MicroK8s: DaemonSet + hostPort, because there is no LoadBalancer.
controller:
  kind: DaemonSet
  hostPort:
    enabled: true
    ports: {http: 80, https: 443}
  service:
    enabled: true
    type: NodePort
