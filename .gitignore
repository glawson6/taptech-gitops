# local scratch
*.local.yaml
*.tmp
.DS_Store
# rendered output — never commit build artifacts
/rendered/
# secrets must never be committed in plaintext
*.dec.yaml
secret-*.yaml
!*sealed*.yaml
# operator env files -- hold service-account tokens (1P, cloud, etc.)
.env
.env.*
!.env.example
# `secret-stores.yaml` is an ArgoCD Application manifest wiring the ESO
# ClusterSecretStore -- structural, no credentials. Un-ignore it.
!secret-stores.yaml

# staged for removal - delete this folder
_to_delete/
