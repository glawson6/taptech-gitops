apiVersion: v1
kind: Service
metadata:
  name: taptech-gitops-agent-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: taptech-gitops-agent-service
  ports:
    - name: http
      port: 80
      targetPort: http
