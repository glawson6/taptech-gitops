apiVersion: v1
kind: Service
metadata:
  name: customer-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: customer-service
  ports:
    - name: http
      port: 80
      targetPort: http
