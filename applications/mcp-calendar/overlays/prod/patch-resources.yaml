apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-calendar
spec:
  template:
    spec:
      containers:
        - name: app
          resources:
            requests: {cpu: 250m, memory: 1Gi}
            limits:   {memory: 2Gi}
