apiVersion: v1
kind: ServiceAccount
metadata:
  name: mcp-calendar
# Calendar-server only talks to Redis (auth via env). No in-cluster RBAC.
automountServiceAccountToken: false
# Sourced from 1Password by the ExternalSecret in platform/app-namespaces.
imagePullSecrets:
  - name: registry-pull
