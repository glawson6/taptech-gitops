apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: taptech-prod
resources:
  - ../../base
  - ingress.yaml

labels:
  - includeSelectors: false
    pairs:
      taptech.net/env: prod

replicas:
  - name: reporting
    count: 2

patches:
  - path: patch-env.yaml
  - path: patch-resources.yaml

# ---- Jenkins writes this line ----
#   kustomize edit set image registry.taptech.net/taptech/reporting=registry.taptech.net/taptech/reporting:<tag>
# Keep it immutable (git sha or build number), never 'latest'.
images:
  - name: registry.taptech.net/taptech/reporting
    newTag: "0.0.0-bootstrap"
